{
  "buildCommand": "npm run build",
  "outputDirectory": "frontend/.next",
  "ignoreCommand": "git diff --quiet HEAD~ HEAD -- . ':!frontend'",
  "env": {
    "NEXT_PUBLIC_API_BASE_URL": "@database_url"
  }
}
