[build]
  command = "npm install"
  publish = "dist"

[env.production]
  NODE_ENV = "production"

# Disable secret scanning since .env is dev-only (not committed to repo)
SECRETS_SCAN_ENABLED = "false"
